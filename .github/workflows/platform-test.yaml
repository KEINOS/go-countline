# This workflow runs the unit test (`go test ./...`) on various platforms,
# such as: Linux, macOS and Windows.
name: Test on macOS/Win/Linux

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main

jobs:
  coverage:
    name: Platform test

    strategy:
      matrix:
        platform: [ubuntu-latest, macos-latest, windows-latest]

    runs-on: ${{ matrix.platform }}

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v6
        with:
          go-version: '1'
          check-latest: true

      - name: Download Modules
        run: go mod download

      - name: Run unit tests
        run: |
          go generate ./...
          go test -race ./...

      - name: Cleanups
        shell: bash
        run: rm -rf ./cl/testdata/data_*.txt
